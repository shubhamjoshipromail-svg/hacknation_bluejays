import { describe, expect, it, vi } from "vitest";
import { decodeVin } from "./vin-service.js";

describe("VIN decoding",()=>{it("normalizes an official vPIC response and preserves ADAS evidence",async()=>{const fetcher=vi.fn(async()=>new Response(JSON.stringify({Results:[{ErrorCode:"0",ErrorText:"0 - VIN decoded clean",ModelYear:"2021",Make:"VOLKSWAGEN",Model:"Tiguan",Trim:"SE",BodyClass:"SUV",VehicleType:"MPV",ForwardCollisionWarning:"Standard",LaneDepartureWarning:"",LaneKeepSystem:"",AdaptiveCruiseControl:"Standard",DynamicBrakeSupport:"Standard"}]}),{status:200,headers:{"content-type":"application/json"}}));const result=await decodeVin("3vv2b7ax0mm103995",fetcher as typeof fetch);expect(result).toMatchObject({vin:"3VV2B7AX0MM103995",year:2021,make:"VOLKSWAGEN",model:"Tiguan",adasLikely:true,source:{classification:"VERIFIED_VIN_DECODE"}});expect(result.adasEvidence).toContain("Forward collision warning: Standard")});it("rejects malformed VINs before making a request",async()=>{const fetcher=vi.fn();await expect(decodeVin("not-a-vin",fetcher as typeof fetch)).rejects.toThrow(/17 characters/);expect(fetcher).not.toHaveBeenCalled()})});
